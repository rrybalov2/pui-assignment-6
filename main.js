/* Object Constructor*/

/* Starting a JQuery document ready function to make sure the DOM is ready for JS */
$( document ).ready(function() {
    console.log( "ready!" );

/* End of doc ready */
});

/* Initializes AOS Animations Lib */
  AOS.init({
    easing: 'ease-in-out-sine'
  });